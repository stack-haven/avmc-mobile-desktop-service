# _react-native-ci.yml
#
# React Native 应用 CI（可重用工作流）
# 触发条件：dorny/paths-filter 检测到 RN 应用改动
#
# ⚠️ 此为占位工作流。avmc-react-native-app skill 实战验证前，CI 配置保持骨架状态。
# 详细使用见：.agents/skills/avmc-react-native-app/SKILL.md

name: react-native-ci

on:
  workflow_call:
    inputs:
      app:
        description: 'RN 应用名（apps/<app>）'
        required: false
        type: string
        default: ''

jobs:
  placeholder:
    name: ⚠️ RN skill 待实战验证
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 检查 RN 应用
        run: |
          echo "⚠️ avmc-react-native-app skill 实战验证前，CI 工作流仅作占位"
          echo "TODO: 集成 ESLint + jest + react-native-testing-library"
          echo "详见 .agents/skills/avmc-react-native-app/SKILL.md"
